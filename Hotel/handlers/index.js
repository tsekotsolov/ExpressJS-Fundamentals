const homeHandler = require('./homeHandler')
const hotelHandler = require('./hotelHandler')
const usersHandler = require('./usersHandler')
const listHandler = require('./listHandler')
const aboutHandler = require('./aboutHandler')
const categoryHandler = require('./categoryHandler')

module.exports = {
  homeHandler,
  hotelHandler,
  usersHandler,
  listHandler,
  aboutHandler,
  categoryHandler
}
